{
  "name": "ssr-react-app",
  "version": "1.0.0",
  "scripts": {
    "build:client": "node esbuild.config.js client",
    "build:server": "node esbuild.config.js server",
    "build": "npm run build:client && npm run build:server",
    "start": "npm run build && node build/server.js"
  },
  "dependencies": {
    "esbuild": "^0.25.6",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.1.0",
    "vite": "^4.5.0"
  }
}
