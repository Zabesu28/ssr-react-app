{
  "name": "ssr-react-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && esbuild server/server.jsx --bundle --platform=node --format=cjs --outfile=build/server.js",
    "start": "node build/server.js",
    "serve": "npm run build && npm start"
  },
  "dependencies": {
    "express": "^4.18.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.1.0",
    "esbuild": "^0.19.11",
    "vite": "^4.5.0"
  }
}
